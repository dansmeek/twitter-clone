#!/usr/bin/perl.bin

def headers(values)
	this.ip = 10.66.86.253
	this.smtp = smtp.gmail.com #incorrect
	this.login = dansmeek@gmail.com
	this.pw = sha256(#encryptPW!)
	myvalues = @this
	return myvalue
end


def getsender(time)
	while (sender.isnil)

	
		@a = headers(encrypt(spoof(@val)))
		do |@a| as |sender|
			b = sender.get_current_sender	
			c = sender.get_current_ip
			d = sender.get_current_spoof_email_address
			e = sender.get_current_sender_domain
			f = sender.get_email_key
		
			curr_arr([b,c,d,e,f])
		end
	end
	returnarray = [curr_arr, time]
end

def get_email_key(src)
	x = extract_from_src(src)
	x = sanetize(key => :find
end

def get_current_ip

end

def get_current_spood_email_address

end

def get_current_sender_domain

end
